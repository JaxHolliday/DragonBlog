@model DragonBlog.Models.Blog

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>
<a asp-action="Create" asp-controller="Posts" asp-route-id="@Model.Id" class="btn btn-primary">Create New Post</a>
<div>
    <h4>Blog</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.URL)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.URL)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-dark btn-sm">Edit</a> 
    <a asp-action="Index" class="btn btn-outline-primary btn-sm">Back to List</a>
</div>
